[project]
name = "text2video"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.12",
    "loguru>=0.7.3",
    "moviepy>=2.2.1",
    "python-dotenv>=1.1.0",
    "uvicorn>=0.34.2",
]

[project.optional-dependencies]
inference = ["accelerate>=1.2", "diffusers>=0.32", "pillow>=10", "torch>=2.2", "transformers>=4.47"]
test = ["httpx>=0.28", "pytest>=8"]
